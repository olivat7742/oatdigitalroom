node_modules/
dist/
.vite/
*.local
.env
.env.*
!.env.example
# .env.mock is NOT excepted here. It holds one line and no secret, but the pre-commit hook
# blocks every .env file on principle and that guard is worth more than the convenience of
# committing this one. See app/README.md for the line that recreates it.
